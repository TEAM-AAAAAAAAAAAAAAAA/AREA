generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "mongodb"
    url      = env("MONGODB_URI")
}

model User {
    id        String   @id @db.ObjectId @map("_id")
    email     String   @unique
    name      String
    password  String
    createdAt DateTime @default(now())
}